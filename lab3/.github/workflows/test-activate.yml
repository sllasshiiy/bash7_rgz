name: Test Activation

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check Actions
        run: echo "🎉 GitHub Actions активированы!"